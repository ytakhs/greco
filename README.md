greco
===

## Usage

```shell
$ greco tags

Usage: greco tags [options] <owner> <repo>

  -p int
    	 (default 50)
  -page int
    	 (default 1)
  -per int
    	 (default 50)
  -t string

  -token string
```
